!!! 5
html
	head
		title r/funny viewer
		link(rel = "stylesheet", type = "text/css", href = "style.css")
		
	body
		
		- foreach(entry; data)
			div.entry
				<h3><a href = "#{entry.permalink}">#{entry.title}</a></h3>
				p By #{entry.author}
				p #{entry.score} votes
				<a href = "#{entry.original}">img(src= "#{entry.url}", alt= "#{entry.url}", title= "#{entry.url}")</a>
				hr()
